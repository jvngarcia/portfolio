import type { ReactNode } from "react";


export type TextDecoratorColor = {
    from: string,
    to: string,
    children: ReactNode,
}

export type CategoriesType = {
    from: string,
    to: string,
    children: ReactNode,
}